name: Lint Chat Server

on:
  push:
    branches:
      - develop
      - main
    paths:
      - "chat-server/**/*.py"
      - "chat-server/pyproject.toml"
      - "chat-server/uv.lock"
      - "pyproject.toml"
      - "scripts/lint-all.sh"
      - ".pre-commit-config.yaml"
      - ".github/workflows/lint-chat-server.yml"
  pull_request:
    types:
      - opened
      - synchronize
    paths:
      - "chat-server/**/*.py"
      - "chat-server/pyproject.toml"
      - "chat-server/uv.lock"
      - "pyproject.toml"
      - "scripts/lint-all.sh"
      - ".pre-commit-config.yaml"
      - ".github/workflows/lint-chat-server.yml"

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Install uv
        uses: astral-sh/setup-uv@v7

      - name: Set up Python
        working-directory: chat-server
        run: uv python install

      - name: Install dependencies
        working-directory: chat-server
        run: uv sync --group dev --index pytorch-cu128=https://download.pytorch.org/whl/cpu

      - name: Run lint
        working-directory: chat-server
        run: uv run bash scripts/lint.sh
